let P = []; 
let t = [];
let r; //materi kelas: input user
let k; //tugas : r dan k input user
let P0 = 20;
let dt = 0.1;
let tmax = 10;

let grafik;

function setup() {
  createCanvas(400, 400);
  
  r = createInput(400, 400);
  r.position()
}

function draw() {
  background(220);
}